import { PricingClient } from "@/components/PricingClient";

export const metadata = {
  title: "充值方案 · Image Studio",
  description: "查看 Image Studio 的充值方案、兌換碼與邀請獎勵。",
};

export default function PricingPage() {
  return <PricingClient />;
}
